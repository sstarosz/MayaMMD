# Auto-generated by CMake from git tags - do not edit.
# Source of truth: git describe --tags
from __future__ import annotations

__all__ = ["__version__", "__version_tuple__", "version", "version_tuple"]

version: str = "@MMD_PROJECT_VERSION@"
__version__: str = version

# Split into (major, minor, patch, ...) - trailing non-digit components stay as str
_version_parts = __version__.split(".")
_version_parsed: list[int | str] = []
for p in _version_parts[:3]:
    _version_parsed.append(int(p) if p.isdigit() else p)
# Append any extra segments (dev, commit hash, etc.) as-is
if len(_version_parts) > 3:
    _version_parsed.extend(_version_parts[3:])

__version_tuple__ = tuple(_version_parsed)
version_tuple = __version_tuple__
