# VPD pose file list — populate with your own .vpd files for testing.
# Place .vpd files in assets/poses/ (see assets/poses/README.md).
# Run `python scripts/database/generate_file_list.py` to regenerate this list.

ALL_VPDS: list[str] = []
